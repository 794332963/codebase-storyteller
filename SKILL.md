---
name: codebase-storyteller
description: 仅在用户显式输入 `$codebase-storyteller` 时使用。支持业务视角与架构（源码/框架）视角，并可从指定 Git ref 或当前工作区读取真实代码，生成详细、中文、图文并茂的离线报告。
metadata:
  version: "2.6.0"
  output: "offline self-contained HTML by default, Markdown on request"
---

# Codebase Storyteller

仅在用户显式输入 `$codebase-storyteller` 时执行。一次只生成一个视角报告；不要因“业务梳理”“代码说明”等普通请求自动激活。

## 0. 维护铁律

**所有对本 skill 的新增、修复、样式调整和质量门槛，必须以适配性与通用性为第一准则。**

- 先把用户反馈抽象成可适用于任意语言、任意架构、任意仓库、任意业务或框架库的规则，再修改 skill。
- 禁止把某次报告中的业务名、模块名、目录、字段、状态、服务名、颜色组合或调用链硬编码为通用预期。
- 允许用具体报告做回归验证，但不得只对该报告打补丁；同类问题必须在模板、规格或主流程规则层获得通用修复。
- 当“某个案例的视觉偏好”和“通用规则”冲突时，保留可配置、可迁移的通用表达，不能降低其他项目的真实性、可读性或视觉质量。
- 任何变更不得让既有业务视角产出在真实性、详实度、静态 SVG、模板一致性、导航简洁性或视觉质量上退化。
- 单次报告、截图或用户反馈只能作为回归样本，不得成为规则本身。修复前必须先将问题归因到可跨仓库、跨语言、跨视角复用的类别，例如布局约束、SVG 构件、响应式策略、证据组织或交互边界；规则中不得出现该案例的业务名、仓库名、实体、状态、方法、坐标、颜色组合或图表内容。
- 优先修复共享规范、模板或可复用生成机制；不得以直接修改某一份已生成报告替代 skill 修复。确需修复既有报告时，必须在完成通用修复后将其作为回归产物重新生成或验证，不能把报告补丁反向写成通用规则。
- 对视觉问题，先区分“共享构件层”与“视角表达层”：前者修复色板、marker、连线、节点、泳道、响应式和可读性，后者保留各视角既有章节、图类型、链路和叙事。不得借一次案例的视觉问题缩减、合并或重写任一视角的表达基线。

## 1. 仓库配置

公开版的 `SKILL.md` 不保存用户机器路径。实际持久化配置固定存放在 skill 根目录的 `config.local.yaml`，该文件被 Git 忽略，不能提交到公开仓库。

```yaml
repository_roots: # 可配置多个根目录，按顺序查找 {root}/{repository_name}
  - /workspace
repository_root: "" # 旧版兼容字段；仅在 repository_roots 为空时使用
repository_paths: {} # 例如 { order-service: /Users/me/Projects/order-service }
target_branch: auto # auto 表示读取目标仓库的默认分支；也可指定 main、release 等
code_source: ref # 可选：ref | worktree；未配置时默认 ref
editor:
  kind: vscode # vscode | cursor | jetbrains
  uri_template: "vscode://file/{path}:{line}:1"
  fallback_command: "code --goto '{path}:{line}:1'"
```

配置规则：

- 每次运行先读取 `config.local.yaml`，不得把公开的示例块当作未配置的本机状态。
- `config.local.yaml` 存在且字段完整时，直接复用；同一会话或后续调用不得重复询问仓库根目录或编辑器。
- `repository_paths[仓库名]` 优先于所有根目录，可用于仓库不在常规目录或同名仓库消歧。
- 依次在 `repository_roots` 中查找 `{root}/{仓库名}`；仅命中一个 Git 仓库时使用它。
- 命中多个同名 Git 仓库时，停止并列出候选绝对路径，要求用户在 `repository_paths` 中添加精确映射；不得擅自选择。
- `repository_roots` 为空时兼容读取旧版 `repository_root`。
- 根目录列表和映射都为空且目标仓库没有映射时，停止并只询问：`你的代码仓库都放在哪个本地路径下？`
- 得到用户路径后，将新路径追加到 `repository_roots`（不覆盖既有路径），再继续执行；配置缺失时不得扫描仓库或生成报告。
- 默认目标分支是 `auto`：读取目标仓库的默认分支。用户可将 `target_branch` 固定为 `main`、`release` 或其他分支；单次调用明确指定分支时，以用户指定为准。
- `code_source` 是默认代码来源：`ref` 通过 Git ref 读取，`worktree` 直接读取当前工作区文件。未配置时为 `ref`。调用显式来源优先于该默认值；但调用未显式来源且当前 HEAD 与目标 ref 不一致时，必须按 Step 1 询问用户，不得静默使用配置默认值。
- `editor.kind` 是必配项。首次配置仓库后若未配置编辑器，停止并询问：`你默认使用哪个代码编辑器：VS Code、Cursor，还是 JetBrains IDE（IntelliJ IDEA、GoLand 等）？`
- 在 macOS 上优先用 `open -Ra` 检查用户选择的应用是否已安装，再写回配置；检测到多个编辑器时仍以用户选择为准。
- `vscode` 使用 `vscode://file/{path}:{line}:1`，`cursor` 使用 `cursor://file/{path}:{line}:1`。
- `jetbrains` 默认使用命令行启动器作为回退：`idea --line {line} --column 1 {path}`；只有用户提供已验证的 JetBrains URL 模板时，才生成可点击 URI。

## 2. 资源与加载顺序

主流程只保留执行控制。开始生成前按需读取以下资源：

| 资源 | 何时读取 | 用途 |
|---|---|---|
| [references/report-spec.md](references/report-spec.md) | 已完成代码定位后 | 报告章节、文字详细度、证据与完整性标准 |
| [references/visual-style.md](references/visual-style.md) | HTML 输出时 | 离线内嵌 SVG、布局、交互、无障碍和视觉规则 |
| [templates/report.html](templates/report.html) | HTML 输出时 | 直接替换占位符并填充报告内容 |
| [AGENTS.md](AGENTS.md) | 需扩展语言或检索规则时 | 设计边界与扩展方式 |

不要把这些资源的全文复制回 `SKILL.md`，也不要跳过它们。

## 3. 输入约定

用户以如下形式调用：

```text
$codebase-storyteller
仓库：repo-a, repo-b
视角：业务 或 架构，可选
业务：可选
目标：可选，仅架构视角使用
主题：可选，报告标题与文件名
格式：html 或 md，可选，默认为 html
分支：可选，默认使用配置的 target_branch
来源：工作区 或 指定分支，可选
要求：可选，自由文本
```

输入约束：

- `仓库` 必填，可多个。
- `视角` 可选值为 `业务`、`架构`。显式字段始终优先；未指定时，先按用户的整体语义意图推断，不能只依赖某个词的机械匹配。
- 倾向于解释业务目的、功能、流程、运行方式、业务逻辑或“某某业务”的请求，推断为业务视角；倾向于解释源码、代码结构、框架、执行内核、实现方式、调用链、扩展点、设计模式或阅读路径的请求，推断为架构视角。上述信号是可迁移的意图示例，不是固定关键词表，禁止把任何具体仓库名、业务名或字符串精确匹配硬编码为规则。
- 推断命中后直接采用对应视角，不单独等待确认；在开始定位前用一句话声明“按业务视角生成…”或“按架构视角生成…”，给用户留出即时纠正机会。
- 只有输入既没有显式 `视角` 字段、又没有可推断的业务或架构意图时，才询问一次二选一的视角选择，并用一句简短理由说明“需要确定报告组织方式后才能定位代码”；在此之前不得扫描、定位或生成。
- 业务视角使用 `业务` 字段；未提供时，必须先列出核心/重要业务并等待用户选择；不得擅自挑一个业务生成。
- 架构视角使用 `目标` 字段，表示待讲解的核心子系统、主干流程或核心抽象。不得将业务名自动当作架构目标；未提供时按 Step 2 列出候选并等待选择。
- `主题` 是报告的展示名称，不参与代码检索、范围判断或机制拆分。明确提供时必须原样作为报告标题，并在安全字符转换后作为文件名。
- 未提供 `主题` 时：业务视角使用业务名称；架构视角只有一个简短目标时使用该目标；架构视角有多个目标时，生成一个概括其共同系统或能力的简洁主题，禁止把逗号分隔的目标列表、文件路径、仓库名或版本号拼接成标题或文件名。所有具体目标保留在“阅读范围”和正文小节中。
- `来源` 可选值为 `工作区`（`worktree`）和 `指定分支`（`ref`）。`工作区` 直接读取目标仓库磁盘上的当前文件；`指定分支` 按 `target_branch` 或调用中的 `分支` 通过 Git ref 读取。用户显式指定时直接执行。
- 兼容既有 `分支：<name>`：未同时指定 `来源` 时，它等同于显式选择 `指定分支`。`来源：工作区` 与 `分支` 同时出现时，停止并要求用户消除冲突，不得猜测分支字段是说明还是读取目标。
- `要求` 是从属指令，用于承接语言、章节内侧重与排序、既有图类型内取舍、讲解语气、读者假设、深浅分配或不破坏骨架的补充小节等自由文本诉求。它只能在本 skill 护栏内添加或微调，不能覆盖任何护栏。
- 下列护栏永远优先于 `要求`：真实性与 `file:line` 可追溯、当前视角的固定章节与方法五要素、全部阻断式质量门槛（含多仓库归属与关系、关系图、状态机、核心时序图、模板/静态 SVG/离线/导航规则）以及通用性。冲突部分直接忽略，其余可满足部分照常采纳。
- `要求` 不得关闭证据、删减必需章节或图、粘贴大段源码直译、突破模板/离线/视角/导航/多仓库规范、要求从代码之外作答，或引入任务专属硬编码。
- `格式` 只有 `html` 和 `md`；默认 `html`。
- 不向用户询问输出路径。始终写入本 skill 的 `reports/` 目录。
- 文件名使用 `{报告主题}.{html|md}`。将空格和路径分隔符替换为 `-`，移除其他不安全文件名字符；不附加仓库名、目标列表、版本号、日期或序号。多仓库范围通过页首元信息与“阅读范围”表达，不进入文件名。
- 同名文件直接覆盖，不加 `v1`、日期或其他后缀。

## 4. 执行流程

### Step 0: 配置检查

1. 读取 skill 根目录的 `config.local.yaml`。
2. 按 `repository_paths -> repository_roots -> repository_root` 的优先级解析每个仓库的绝对路径。
3. 验证目录存在且是 Git 仓库。路径无效时列出无效仓库和解析路径，停止等待用户修正。
4. 仓库或编辑器配置缺失时按第 1 节提问并持久化配置，不进行后续动作。
5. 配置就绪后解析 `视角`：显式指定时直接采用；未指定但可从整体措辞推断时直接采用并在开始定位前声明所用视角；仅在无明确语义信号时才询问一次业务/架构二选一。该推断不影响后续来源与分支歧义检查，真实版本歧义仍按 Step 1 询问。

### Step 1: 代码来源与版本快照

无论来源为何，绝不执行 `git checkout`、`git switch`、`git reset` 或改动用户工作区。

1. 先确定是否存在显式来源：`来源：工作区` 直接选择 `worktree`，不解析 `origin/HEAD`、目标 ref 或其 tree；`来源：指定分支` 或单独给出 `分支` 选择 `ref`。两者冲突时停止并要求用户消除冲突。
2. 仅在来源为 `ref` 或调用未指定来源时，为每个仓库解析目标 ref：`target_branch=auto` 时优先 `refs/remotes/origin/HEAD`，其次当前本地 HEAD 指向的分支；指定分支时依次确认 `refs/heads/<target_branch>`、`refs/remotes/*/<target_branch>`。指定分支不存在时回退到默认 ref，并记录回退原因。
3. 调用未显式 `来源` 时，解析每个仓库当前 HEAD commit 并与其目标 ref commit 比较。任一仓库不一致时，停止并询问用户读取“当前工作区已 checkout 的代码”还是“配置的 ref”；不得因 `code_source` 默认值自动选择。全部一致时才使用 `code_source` 默认值，未配置则使用 `ref`。
4. 来源为 `ref` 时，使用解析到的 ref tree 和 blob 完成搜索、读取与行号计算；目标分支内容是唯一证据，不使用工作区未提交修改。
5. 来源为 `worktree` 时，直接读取目标仓库工作区磁盘上的当前文件完成搜索、读取与行号计算。工作区当前内容（包括未提交修改）是本次证据；显式工作区模式不解析或读取 `origin/HEAD` 内容。
6. 工作区模式为每个仓库记录实际 checkout 标识：附着分支优先显示 `git rev-parse --abbrev-ref HEAD`，detached HEAD 显示 `git describe --tags --always` 的 tag 或 short SHA。多仓库分别记录，不假设版本一致。
7. 报告页首元信息只展示标题、涉及仓库、分析分支和生成日期；`ref` 模式在“分析分支”展示每个仓库实际读取的 ref，`worktree` 模式展示每个仓库实际 checkout 标识，并通过模板 `{{SOURCE_NOTICE}}` 明确注明“行号与当前工作区一致”。跨仓库图注和说明中也使用各参与仓库的实际版本标识。

除工作区模式外，不要把当前工作区未提交改动当作证据。

### Step 2: 按视角自动定位代码

用户不需要提供文件路径。先探测实际语言、框架、入口方式和项目布局，再将发现映射到概念角色；不得假设 DDD、MVC、微服务、单体或固定目录结构。

- 入口角色：HTTP/gRPC/CLI/消息入口/定时入口或等价机制。
- 核心角色：推动业务规则、状态或事务的函数/类/模块。
- 异步角色：消息发布/消费、任务、调度、回调或等价机制。
- 外部依赖角色：DB、文件、HTTP/RPC、缓存、MQ、第三方 SDK 或等价机制。
- 实体角色：业务对象、记录、DTO、schema、表模型或等价数据结构。

概念角色只用于组织分析，不要求每个仓库都存在。自动识别实际语言与布局，覆盖 Go、Java/Kotlin、TypeScript/JavaScript、Python、Rust、SQL；语言线索见 `AGENTS.md`。

#### 业务视角

业务视角底层视觉构件继承 `references/visual-style.md` 的“共享构件层（两视角共用）”；其表达层规则独立，既有章节、图表、方法五要素、真实性门槛和 HTML 交付质量均不得降低，也不受架构视角表达规则影响。

给了业务名称时：

1. 用业务名、模块名、路由、消费者/任务名、实体名和相邻调用图检索。
2. 沿调用方向扩展到入口、领域、存储、异步与外部依赖。
3. 只纳入能够直接支撑该业务主干、取消或修复链路的文件。

未给业务名称时：

1. 扫描对外路由、RPC/HTTP handler、MQ consumer、定时任务和模块包名。
2. 按入口数量、关联核心代码规模和模块边界识别并排序“核心/重要”业务；忽略纯工具、配置和琐碎 CRUD。
3. 每项给出业务名、所属仓库、主要入口和一句话说明。
4. 等待用户选择一个/多个业务或输入“全部生成”。收到选择后再继续。

#### 架构视角

架构视角底层视觉构件继承 `references/visual-style.md` 的“共享构件层（两视角共用）”；其表达层规则独立，面向源码阅读、框架机制与可扩展性讲解，不得套用业务实体、状态或上下游叙事，也不得反向改变业务视角表达。

给了 `目标` 时：

1. 用目标名称、包/模块名、公开 API、类型名、接口/trait/abstract class、注册点、调用图和相邻依赖检索。
2. 沿调用方向扩展到入口 API、编排/执行器、核心抽象、实现、构建/解析、存储或网络边界、注册与扩展点。
3. 只纳入直接支撑该目标主干与扩展机制的文件；不为覆盖层次而纳入无关业务模块。

未给 `目标` 时：

1. 扫描公开 API、CLI/服务启动点、路由/RPC 注册、工厂/容器装配、接口或 trait 实现、插件/回调/中间件注册、解析/执行/存储主干。
2. 根据可达入口、核心调用图规模、抽象层次和扩展边界，识别并排序核心子系统、主干流程或核心抽象；名称和说明必须来自真实代码，不预设 ORM、插件、schema、缓存或迁移等类别。
3. 每项给出目标名、所属仓库、主要入口/核心类型和一句话说明。
4. 等待用户选择一个/多个目标或输入“全部生成”。收到选择后再继续。

完成定位后打印：

`已定位 X 个相关文件，开始阅读并分析...`

定位完成后先按 `references/report-spec.md` 的“深度与覆盖闸门”评估核心单元与范围。无论范围大小，始终生成一份完整报告；范围增长时报告体量随之增长，每个单元独立成节并保持完整实现面、图表可读性和固定质量门槛。不得默默抽样、合并不同机制、压薄内容或拆分为多份报告。

### Step 3: 结构化读取与证据建模

先读完全部相关文件，再形成结论或输出报告。维护一个内部证据账本，每个事实都至少记录 `仓库 / branch / file / line / symbol`。

两种视角都继承 `references/report-spec.md` 的“深度与覆盖闸门”：先完整阅读与所选范围相关的全部文件再输出；为每个核心单元记录涉及的核心文件与符号集合，并覆盖其完整实现面中的关键类型、方法、分支、包和跨仓库边界。提取内容均以真实代码为准；某项在范围内不存在时记录“代码范围内未发现”，不得为凑章节、图表或门槛编造内容。

业务视角提取实体、状态机、业务流程、上下游和字段生命周期。

架构视角提取公开入口与调用链、核心抽象和类型关系、关键数据结构与构建/流转、装配/注册/配置边界、实现替换点、错误传播和扩展机制。状态机仅在代码存在且确实解释该架构目标时作为支持性内容呈现；无状态实体或执行模型必须写“代码范围内未发现”，不得虚构业务状态机。

多仓库分析默认保留每条证据的仓库、来源和实际版本标识。检索 import/模块依赖、构建替换、注册、接口实现、调用、复制或改造证据来识别仓库关系；仅在代码证明时陈述“基于、扩展、改写、覆盖或调用”，未证明时保持独立说明。代码没有证明的关系、状态或意图不得绘制或陈述为事实；真实性优先于任何报告门槛。

### Step 4: 生成报告

1. 读取 `references/report-spec.md`。
2. HTML 模式额外读取 `references/visual-style.md` 与 `templates/report.html`。
3. HTML 必须复制 `templates/report.html` 后仅填充占位符，禁止另写 HTML 骨架、布局容器或样式体系。
4. 只能使用模板已有 class；不得创建替代骨架 class（例如 `.diagram`、`.code`、`.nav`）。
5. 所有图在写入时即为最终 inline SVG，含最终配色、泳道、分组、箭头和文字。禁止用 JavaScript 重建、替换、重着色或后处理 SVG；禁止死代码和备用 SVG 字符串。
6. HTML JavaScript 只允许模板已有的 scrollspy 和 lightbox。
7. 两种视角都必须直接复用 `templates/report.html` 的唯一 `<head><style>`，不得保留或生成视角专属 head CSS；SVG 内部颜色仅由固定 token 的内嵌标准图元套件、class 或内联 `style` 控制，外层 CSS 不得控制 SVG 节点颜色。
8. 每张图的 `.diagram-zoom` 后不得输出 `<p class="muted">来源：…</p>`、`.evidence` 或其他图下来源/证据行；图内归属小字及正文/表格的 `.code-open` 深链保留。
9. 方法名或明确对象名本身必须是编辑器 deep link，使用 `.code-open` 与 `target="_blank"` 呈现；不显示“打开代码”泛化文案、fallback 命令或折叠代码片段。
10. 先完成第 5 节阻断式自检，再一次性写出报告；不要边读边输出报告正文。
11. Markdown 模式使用 Mermaid 代码块、普通代码块和锚点目录；条件性章节与详细度规则与 HTML 相同。
12. 自动创建 `reports/` 目录（若不存在），并按第 3 节的固定命名规则覆盖写入。
13. 无论报告是首次生成还是覆盖同名已有文件，完成提示都必须输出该报告的可点击文件链接，格式为 `[报告文件名](/绝对路径/报告文件名)`。
14. 报告标题、侧边栏品牌和文件名均以“报告主题”为主；业务名称或架构目标仅在未提供主题时用于推导主题。仓库名只作为范围和分支的辅助信息出现，绝不附加到文件名。
15. 两种视角的章节集和方法五要素必须严格按 `references/report-spec.md` 的对应规范生成。
16. 有 `要求` 时，在完成定位和证据建模后评估其可采纳范围：先生成满足视角骨架和阻断门槛的报告计划，再仅在允许范围内调整表达与补充内容。完成提示简要列出已采纳项、受护栏限制而调整的项，以及因冲突未采纳项和一句原因。

## 5. 阻断式质量门槛

写文件前逐项检查。**某要素在真实代码存在而未呈现时，必须停下补读/补齐；某要素在真实代码范围内不存在时，明确写“代码范围内未发现”后可跳过。绝不为通过门槛编造。**

- 所有结论、节点、方法、状态、外部调用和字段变化均可回溯到选定代码来源的真实 `file:line`。
- 两种视角均通过“深度与覆盖闸门”：每个核心单元覆盖完整实现面而非少量样本；阅读范围如实反映实际覆盖的文件规模与单元文件/符号集合；无论范围大小均保持单份完整报告，不存在为控制篇幅而抽样、合并机制或压薄单元。
- 多仓库时，页首“涉及仓库”逐一展示 `仓库名 @ 实际版本/ref`；结论、方法、章节、图元和跨仓库边均可识别所属仓库。单仓库时不增加多仓库标签或额外图例。
- 多仓库关系只有在 import/依赖声明、构建替换、注册、实现、调用或可验证改造证据存在时才显式呈现；底座、扩展、fork、改造版或上层仓库分别叙述，除非代码证明共享行为才合并陈述。
- 系统/架构总览、实体/类型关系图和时序图在多仓库时必须呈现仓库边界与跳转；跨仓库方法调用、实现或数据传递标注来源仓库与目标仓库。
- 最终 HTML 基于模板占位符，未自造 HTML 骨架或替代 class。
- 业务视角中，核心实体多于一个时 ER 图必须包含每个实体、真实关联字段和方向；没有可证明关联时明确写“代码范围内未发现实体关联”。架构视角中，核心抽象与类型关系图必须包含真实实现、组合、注入或注册关系。
- 类型关系图或包含类型层次的业务 ER 图使用 `references/visual-style.md` 定义的 UML 关系记法；仅画代码可证明的关系，不臆造基数、所有权或方向。
- 有状态实体或执行模型必须各有状态机；不存在时明确说明未发现，架构视角不得虚构业务状态机。
- 每条真实存在的核心链路或架构主干调用链均必须有独立且详实的时序图；不存在则显式说明。
### 构件层自检（两视角共跑）

- 共享构件层的色板、marker、线宽、正交布线、实线/虚线语义、圆角、字号、泳道、字段胶囊、异步分组、决策菱形和图例是否统一；SVG 颜色是否全部由节点内联 `style` 控制，模板 CSS 是否未向 SVG 内部节点写入 `fill`。
- 每张 SVG 的 `<title>` 后是否逐字节嵌入 `references/visual-style.md` 的“标准图元套件（逐字复制到每个 SVG）”；是否不存在自定义 marker 几何或不一致的节点框构件。
- 扫描每个 `<rect>`、`<circle>`、`<polygon>`、`<path>`：其 fill 是否能由标准图元套件中的 class 或元素自身内联 style 解析；任何未定义 class 或未解析 fill 都必须阻断输出，防止默认黑色区域。
- 每张图是否先为主路径、异步/重试、失败/补偿和图例分配独立区域；在宽屏、窄屏横向滚动和 lightbox 中是否无裁切、遮挡、跨区覆盖或通过压缩字号硬塞内容。内容过多时必须增高、拆图或保留滚动，不得删减表达层事实。
- 内容框文字是否未溢出边界、末端距离右边界至少 `10px`；必要时只允许扩大该内容框本身，禁止自动重画色条、相邻节点、同排节点、泳道、分区背景或既有连线。
- 泳道 lane/swimlane、`.async-group`、`.section-bg` 等结构容器是否保留满高背景、原有标签位置与子元素关系；它们永久豁免贴合文字、统一高度和任何自动重排判定。
- 每条有向关系是否为独立带 `marker-end` 的 SVG path；无方向线是否只用于泳道/边框；每条终点是否落在目标框或时序生命线的可见进入区域并计入 `refX`。
- 含分支流程图的判断节点是否全部为居中文字的 SVG `<polygon>` 菱形，普通步骤是否保持圆角矩形，出边是否标注真实条件。
- 同一报告内是否不存在内容实质相同的重复图；需要复用同一对象时，第二张图是否呈现当前章节要求的不同角度。
- 所有图下是否未出现“来源：…”或“证据：…”行；图内归属、正文和表格中的深链是否仍足以追溯关键事实。
- 图元视觉已冻结：后续只迭代内容层，不得新增像素级布局、色条联动、统一高度或容器尺寸规则；共享构件修复也不得改变既有稳定图的布局。

### 表达层自检（按视角各跑）

- **业务视角：**是否完整保留 14 个固定章节、业务既有图类型（泳道流程图、ER 图、状态机、字段生命周期图）、每条真实核心链路的独立详实时序图，以及业务版方法五要素和叙事密度；构件层规则不得作为缩减理由。
- **架构视角：**是否完整保留 12 个固定章节、架构既有图类型（UML 类图、时序图、决策流程图、数据结构流转图、总览图）、每个真实目标/机制的独立小节与图，以及架构版方法五要素和图文密度；构件层规则不得作为缩减理由。
- 流程图中出现的核心方法，都在模块深挖或方法速查表中解释。
- 业务视角的模块深挖与架构视角的核心机制深挖均覆盖真实存在的概念角色；每个真实核心方法答齐当前视角对应的五要素。不存在的角色写明未发现，不凑数。
- 固定章节骨架完整，且必须匹配当前视角；每章只呈现真实内容，缺失要素写“代码范围内未发现”而非留空或注水。
- 架构视角图表密度与同等范围业务视角不低：架构总览、UML 类型关系、关键数据结构流转图均存在；每个真实核心目标、主干或机制各有专属图与对应深挖，不能将不同性质机制压缩进少数共享时序图。
- 架构视角每张图复用模板已有 SVG 绘制约定（包括 `.lane`、`.lane-title`、`.step-badge`、`.field-pill`、`.async-group` 等适用 class），在配色、圆角、字号、图例、留白、正交布线和无重叠方面不低于业务视角。
- 时序图包含每个关键真实步骤的编号、方法名、关键入参、字段写入/返回，并画出真实成功/失败、重试、幂等、异步分支。步骤过多才拆连续图，不得压缩成玩具图。
- 证据标注克制：仅不确定之处使用 `证据不足`。
- “设计亮点”仅在代码存在可回溯、具有解释价值的设计时以模板 `.highlight` callout 呈现；不是固定章节、不是必填项，不得使用主观营销语或为凑数量编造。
- HTML 不存在外部 `http(s)` 资源、暗色主题或空白/损坏图占位符。
- JavaScript 仅含 scrollspy/lightbox，不操作 SVG，不含 `innerHTML` 图表字符串或永不执行分支。
- 编辑器 deep link 的可见文字是方法名或明确信息本身，使用 `.code-open target="_blank"`；不显示 fallback 命令或“打开代码”泛化文案。
- 输出文件存在、非空，且位于本 skill 的 `reports/` 目录并符合固定命名规则。
- `要求` 的采纳未破坏任何真实性、视角骨架、多仓库归属、图表、模板、离线或导航门槛；冲突内容已忽略而非曲解为已满足。

完成后只简要告诉用户：报告的可点击文件链接和输出路径、分析分支、纳入文件数、`要求` 的采纳结果，以及配置位于 `SKILL.md` 第 1 段。该链接为强制输出项：即使同名报告被覆盖，也不得省略。证据不足只能在对应结论旁标注，不单独组成报告章节。
